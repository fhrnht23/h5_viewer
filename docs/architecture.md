# Архитектура H5 Viewer

## Принципы

Приложение разделено на четыре слоя:

```text
presentation/qt  →  application  →  domain
                           ↑
              infrastructure/hdf5
```

`domain` содержит только типы значений, ошибки и протокол `HdfRepository`. Здесь ссылка HDF5
(`LinkRef`) отделена от идентичности целевого объекта. Это необходимо, потому что один объект может
иметь несколько hard links, а группы могут создавать циклы.

`application` управляет открытым документом, командами undo/redo и безопасной рабочей копией.
`DocumentSession` совместно используется обеими панелями, поэтому один файл не открывается двумя
конфликтующими write handles.

`infrastructure/hdf5` реализует доменный протокол через h5py. Ни один объект или handle h5py не
покидает инфраструктурный слой. Публичная операция открывает файл, выполняет ограниченную работу и
закрывает его. Такой подход немного дороже постоянного handle, но упрощает владение ресурсами,
переключение на рабочую копию и тестирование.

`presentation/qt` содержит Qt models/views, две панели, инспектор, темы и локализацию. Бизнес-логика
не импортирует PySide6.

Главное окно целиком отдано двум навигационным панелям. Одиночный выбор обновляет краткую сводку
объекта в status bar, а `Enter` открывает подробный инспектор в отдельном немодальном окне. Поэтому
таблицы данных и атрибутов не уменьшают рабочую высоту Commander-style панелей.

## Модели Qt

`HdfTreeModel` наследует `QAbstractItemModel` и загружает членов группы страницами по 200 ссылок.
Узел хранит множество идентичностей предков; совпадение с целевым объектом новой hard link помечает
цикл и запрещает дальнейшее раскрытие.

`DatasetTableModel` хранит только одну страницу размером не более 100 × 50 элементов. Для N-D
dataset пользователь выбирает ось строк, ось столбцов и фиксированные индексы остальных измерений.
Редактирование одной ячейки преобразуется в application-команду.

Инспектор references разрешает object и region references только внутри короткоживущего файлового
handle, после чего передаёт UI обычные доменные значения. Для reference-valued dataset читаются
не более 64 отдельных элементов; большие массивы не материализуются. Region selection сохраняет
тип, число точек и границы. Dimension scales и VDS mappings читаются как метаданные без чтения
payload; специальная исходная выборка VDS `H5S_ALL` обрабатывается отдельно.

Поиск метаданных обходит links без повторного раскрытия group aliases и циклов. Он проверяет пути,
типы объектов и ссылок, dtype/shape/layout datasets, имена и ограниченные значения атрибутов.
Значение атрибута не читается, если его storage превышает 1 МиБ или число элементов превышает
10 000; это явно фиксируется в предупреждениях отчёта.

Сравнение строит для обоих документов manifests структуры, dataset metadata и SHA-256 небольших
атрибутов. Значения datasets читаются прямоугольными блоками до выбранного бюджета, по умолчанию
4 МиБ. Для chunked datasets проверяется объединение только физически выделенных chunks: пустой
разреженный массив с огромным логическим shape не сканируется целиком. Hard-link aliases одного
dataset не дублируют чтение payload. Для floating-point данных доступны абсолютный и относительный
допуски; object/region references предварительно преобразуются в сравнимые описания целей.

Экспорт также не передаёт h5py handles в presentation. Полный NPY создаётся через NumPy memory map
прямоугольными блоками с предварительной проверкой свободного места. CSV содержит всю projection,
задаваемую выбранными осями и фиксированными индексами инспектора; смещения текущей страницы на
экспорт не влияют. Результат сначала записывается во временный файл рядом с назначением и только
после успешного завершения публикуется через атомарную замену. Отмена и ошибка сохраняют прежний
файл назначения.

Визуализация получает только уже загруженную страницу размером не более 100 × 50 элементов.
Числовой вектор показывается как line plot, матрица — как heatmap, для complex dtype используется
модуль значения. `pyqtgraph` импортируется лениво и остаётся опциональной зависимостью `plots`.

Создание dataset и его расширение также оформлены обратимыми application-командами. Пользовательское
изменение размера допускает только расширение chunked dataset в пределах `maxshape`. Undo уменьшает
его до исходной формы после отмены всех более поздних команд, поэтому исходные данные не теряются.
Произвольное уменьшение потребует отдельного дискового snapshot и пока явно запрещено.

Удаление links также является обратимой командой. Для soft/external link достаточно сохранить её
метаданные. Hard link восстанавливается через другой alias того же объекта; если удаляется последняя
hard link, объект потоково копируется во временный HDF5 snapshot без загрузки payload в память.
Snapshot удаляется вместе с командой при сохранении, Discard или отбрасывании redo-ветки.

Межфайловое перемещение представлено парой команд: копированием в документ назначения и безопасным
удалением из источника. Главное окно связывает их undo/redo и соблюдает порядок восстановления
источника перед удалением копии. Физическое сохранение двух файлов остаётся неатомарным.

## Локализация

Все статические пользовательские строки проходят через `QCoreApplication.translate`. Встроенный
`DictionaryTranslator` предоставляет русский перевод без внешней зависимости от `lrelease`.
Английские source strings являются вторым языком. Выбор сохраняется через `QSettings`, русский
используется по умолчанию.

## Ограничение многопоточности

h5py сериализует обращения к HDF5 глобальной блокировкой. Ограниченные чтения, поиск и сравнение
пока выполняются синхронно; между блоками UI обрабатывает события и позволяет отменить операцию.
Отдельный HDF5-вызов всё ещё может задержать интерфейс. Полная изоляция длительных операций в
рабочих процессах остаётся задачей этапа устойчивости, а не конкурентных h5py-потоков.
